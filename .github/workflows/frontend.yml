name: Pipeline frontend
run-name: Executada por ${{ github.actor }}

on:
  workflow_dispatch:
    inputs:
      frontend_image:
        description: Imagem Docker do frontend
        required: true
        default: reisluiz/community-kitchen-frontend:latest

jobs:
  build:
    name: Build frontend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout frontend
        uses: actions/checkout@v5
        with:
          repository: Lucas-Cast/community-kitchen-portal
          path: frontend

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Instalar dependencias
        working-directory: frontend
        run: npm ci

  push-image:
    name: Push imagem Docker
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Checkout infra
        uses: actions/checkout@v5

      - name: Checkout frontend
        uses: actions/checkout@v5
        with:
          repository: Lucas-Cast/community-kitchen-portal
          path: frontend

      - name: Login no Docker Hub
        run: |
          echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login \
            -u "${{ secrets.DOCKERHUB_USERNAME }}" \
            --password-stdin

      - name: Build da imagem Docker
        run: |
          docker build \
            -f applications/frontend/Dockerfile \
            -t "${{ inputs.frontend_image }}" \
            frontend

      - name: Push da imagem no Docker Hub
        run: docker push "${{ inputs.frontend_image }}"

  deploy:
    name: Deploy com Ansible
    runs-on: ubuntu-latest
    needs: push-image

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Instalar Ansible
        run: |
          sudo apt-get update
          sudo apt-get install -y ansible

      - name: Configurar chave SSH
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' '${{ secrets.VM_SSH_PRIVATE_KEY }}' | sed 's/\\n/\n/g' | tr -d '\r' > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keygen -y -f ~/.ssh/id_rsa > /dev/null

      - name: Configurar IPs do frontend
        working-directory: ansible
        run: |
          sed -i "s/IP_FRONTEND_PROD/${{ secrets.IP_FRONTEND_PROD }}/g" hosts.ini
          sed -i "s/IP_FRONTEND_HOMOLOG/${{ secrets.IP_FRONTEND_HOMOLOG }}/g" hosts.ini

      - name: Deploy do frontend de homolog
        working-directory: ansible
        run: |
          ansible-playbook playbooks/deploy-frontend.yml \
            --limit frontend_homolog \
            --extra-vars "frontend_image=${{ inputs.frontend_image }} gateway_public_ip=${{ secrets.IP_GATEWAY }}"

      - name: Deploy do frontend de prod
        working-directory: ansible
        run: |
          ansible-playbook playbooks/deploy-frontend.yml \
            --limit frontend_prod \
            --extra-vars "frontend_image=${{ inputs.frontend_image }} gateway_public_ip=${{ secrets.IP_GATEWAY }}"
